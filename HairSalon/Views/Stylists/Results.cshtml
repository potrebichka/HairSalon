@{
    Layout = "_Layout";
}

<div id="horizontalMenu">
    <ul>
        <li>@Html.ActionLink("Home", "Index", "Home")</li> 
        <li>@Html.ActionLink("Search stylist", "Search")</li>
    </ul>
</div>

<div class="container contentBox">
    <h2>Found Stylist:</h2>

    @if (Model == null)
    {
        <h3>No stylists have been found.</h3>
    }
    else
    {
        <h3><li>@Html.ActionLink($"{Model.Name}", "Details", new {id = Model.StylistId} )</li></h3>
    }
</div>
